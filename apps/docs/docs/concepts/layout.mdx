---
title: Disposition
slug: /concepts/layout
---

import { DataFlowPlayer } from 'react-dataflow-animator';
import { demosById } from '@site/src/site-content';

La disposition des nœuds repose sur quatre concepts : `direction`, `lane`,
`align_with`, et le mode `circular`. Vous ne fournissez **aucune
coordonnée** — le moteur place chaque nœud en ratios relatifs au conteneur,
ce qui rend l'animation responsive sans calcul manuel.

## `direction`

Cinq valeurs possibles pour `direction` (au niveau racine de la spec) :

| Direction                | Axe principal            | Axe transverse |
| ------------------------ | ------------------------ | -------------- |
| `left-to-right` (défaut) | horizontal               | vertical       |
| `right-to-left`          | horizontal (inversé)     | vertical       |
| `top-to-bottom`          | vertical                 | horizontal     |
| `bottom-to-top`          | vertical (inversé)       | horizontal     |
| `circular`               | autour d'un nœud central | —              |

## Grilles et lanes

En mode linéaire (les quatre premières directions), la propriété `lane`
contrôle la position du nœud **le long du flux**. Les nœuds qui partagent
la même `lane` sont **empilés** sur l'axe transverse.

```ts
{
  direction: 'left-to-right',
  nodes: [
    { id: 'web1', type: 'server', lane: 2 },
    { id: 'web2', type: 'server', lane: 2 }, // même colonne que web1
    { id: 'db',   type: 'database', lane: 3 },
  ],
}
```

Quand peu de lanes sont définies (2-3), les nœuds s'étalent vers les bords
du canevas ; au-delà, ils se resserrent vers le centre.

## Alignement transverse (`align_with`)

`align_with` permet d'aligner un nœud sur l'**axe transverse** d'un autre,
identifié par son `id`. Pratique pour aligner deux nœuds de lanes
différentes :

```ts
{
  nodes: [
    { id: 'client', type: 'laptop', lane: 1 },
    { id: 'api',    type: 'server', lane: 2 },
    { id: 'cache',  type: 'database', lane: 3, align_with: 'api' },
  ],
}
```

Ignoré en mode `circular`.

## Mode circulaire

En `circular`, le nœud marqué `main: true` est placé au centre ; les
autres sont répartis à équidistance sur un cercle :

```ts
{
  direction: 'circular',
  nodes: [
    { id: 'gateway', type: 'server', main: true },
    { id: 'svc-a',   type: 'server' },
    { id: 'svc-b',   type: 'server' },
    { id: 'svc-c',   type: 'server' },
  ],
}
```

<DataFlowPlayer theme="auto" spec={demosById.microservices.spec} />

## Mise à l'échelle responsive

Le composant calcule une **« cellule »** (plus petite distance entre deux
nœuds, en px) qui pilote :

- un facteur d'échelle global (`--rdfa-scale`) : icônes et polices plus
  grandes en plein écran, plus petites si l'espace est serré ;
- la largeur maximale des panneaux et paquets (`--rdfa-maxw`) pour qu'ils
  ne débordent jamais sur les voisins ;
- la position de chaque nœud, bornée selon sa taille mesurée pour éviter
  les débordements du canevas.

La prop `density` (`'compact' | 'comfortable' | 'spacious'`) module ces
réglages pour adapter le visuel à un contexte dense (sidebar) ou aéré
(plein écran).
